###############################################
# 📊 RESUMEN DE ESTADÍSTICA EN R (DOCUMENTADO)
# Incluye teoría para resolver ejercicios
# sin necesidad de ejecutar cálculos
###############################################

# ===============================
# 1. Estadística Descriptiva
# ===============================
# La estadística descriptiva organiza y resume datos.
# Medidas:
# - MEDIA: promedio de los datos.
# - MEDIANA: valor central al ordenar los datos.
# - MODA: valor que más se repite.
# - RANGO: diferencia entre el mayor y menor valor.
# - CUARTILES: dividen en 4 partes (Q1, Q2, Q3).
# - PERCENTILES: dividen en 100 partes.
# - IQR (rango intercuartílico): Q3 - Q1.
# - VARIANZA: dispersión cuadrática.
# - DESVIACIÓN ESTÁNDAR: raíz de la varianza.
# - COEFICIENTE DE VARIACIÓN: CV = (σ / media).

# ===============================
# 2. Tablas de Frecuencia
# ===============================
# - FRECUENCIA ABSOLUTA (FA): cuántas veces aparece un valor.
# - FRECUENCIA RELATIVA (FR): proporción respecto al total.
# - FRECUENCIA ACUMULADA (FAC): suma progresiva de las frecuencias.

# ===============================
# 3. Visualización de Datos
# ===============================
# - HISTOGRAMA: muestra la distribución de una variable numérica.
# - BARRAS: para variables cualitativas.
# - BOXPLOT: muestra mediana, cuartiles y outliers.
#   ✔ Caja = Q1 a Q3
#   ✔ Línea central = mediana
#   ✔ Bigotes = valores extremos sin ser atípicos
#   ✔ Puntos fuera = outliers

# ===============================
# 4. Probabilidad
# ===============================
# - ESPACIO MUESTRAL (Ω): conjunto de todos los resultados posibles.
# - PROBABILIDAD CLÁSICA: P(E) = casos favorables / casos posibles.
# - PROBABILIDAD EMPÍRICA: se calcula con experimentos o simulación.

# ===============================
# 5. Probabilidad Condicional
# ===============================
# Fórmula: 
#   P(A|B) = P(A ∩ B) / P(B)
# ✔ Se interpreta como la probabilidad de que ocurra A dado que ocurrió B.

# ===============================
# 6. Teorema de la Probabilidad Total
# ===============================
# Cuando un evento puede ocurrir por varias causas mutuamente excluyentes:
#
# Fórmula general:
#   P(A) = P(A|B1)P(B1) + P(A|B2)P(B2) + ... + P(A|Bn)P(Bn)
#
# ✔ Clave: se usa cuando el espacio se divide en escenarios (partición).

# ===============================
# 7. Teorema de Valles (o Bayes)
# ===============================
# Permite "invertir" probabilidades condicionales, es decir,
# calcular la probabilidad de una causa dado un efecto.
#
# Fórmula general:
#   P(Bi|A) = [ P(A|Bi) * P(Bi) ] / Σ [ P(A|Bj) * P(Bj) ]
#
# Donde:
# - Bi: cada posible causa (partición del espacio).
# - A: el evento observado.
# - P(Bi): probabilidad previa de la causa.
# - P(A|Bi): probabilidad de observar A si ocurrió Bi.
# - El denominador: probabilidad total de A (teorema de probabilidad total).
#
# ✔ Clave:
# - Numerador: peso del escenario Bi en el evento A.
# - Denominador: normaliza la suma de todas las causas posibles.
#
# Ejemplo conceptual:
#   • Una prueba médica: queremos P(Enfermedad|Positivo).
#   • Se usa Bayes para actualizar el riesgo sabiendo el resultado del test.

# ===============================
# 8. Técnicas de Conteo
# ===============================
# - PERMUTACIONES: número de formas de ordenar elementos.
#   Fórmula: P(n,k) = n! / (n-k)!
# - COMBINACIONES: elegir sin importar el orden.
#   Fórmula: C(n,k) = n! / [k!(n-k)!]
# ✔ Orden importa → Permutación.
# ✔ Orden no importa → Combinación.

# ===============================
# 9. Inferencia Estadística
# ===============================
# Se basa en usar muestras para sacar conclusiones sobre la población.
# - INTERVALOS DE CONFIANZA (IC): rango donde probablemente esté el parámetro.
# - Ejemplo: un IC del 95% para la media indica un 95% de confianza
#   en que la media poblacional esté en ese rango.
# ✔ IC estrecho → más precisión.

# ===============================
# 10. Conceptos Extras
# ===============================
# - RANGO DE VARIACIÓN: valor máximo - valor mínimo.
# - MARCA DE CLASE: punto medio de cada intervalo en tablas agrupadas.
# - CINCO NÚMEROS DE TUKEY: mínimo, Q1, mediana, Q3, máximo.
# - OUTLIERS: valores fuera de [Q1 - 1.5*IQR, Q3 + 1.5*IQR].

# ===============================
# 🔑 Claves Generales para Resolver Ejercicios
# ===============================
# 1. Identificar tipo de variable (cualitativa o cuantitativa).
# 2. Usar medidas de centralización y dispersión adecuadas.
# 3. Graficar datos → ayuda a detectar patrones y outliers.
# 4. Probabilidades:
#    - Usar clásica o empírica según corresponda.
#    - Condicional: restringir al evento dado.
#    - Total: cuando hay varios escenarios.
#    - Valles (Bayes): cuando piden probabilidad "inversa".
# 5. Técnicas de conteo → diferenciar si el orden importa o no.
# 6. Inferencia → comprobar nivel de confianza y supuestos de normalidad.

###############################################
# Con este resumen tienes la teoría completa,
# incluyendo el Teorema de Valles (Bayes).
###############################################
